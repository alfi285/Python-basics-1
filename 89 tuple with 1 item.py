# Create a tuple with one item

# To create a tuple with one item we need to add a coma after the item
# Otherwise python will not recogonize it as a tuple- (not a tuple eg: "tupledta")

thistuple = ("Monkey",)
print("Type : ", type(thistuple))